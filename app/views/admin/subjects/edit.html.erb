<% provide:title, t("admin.subject.edit") %>
<div class="devise-content">
  <h1><%= t "admin.subject.edit" %></h1>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= form_for [:admin, @subject] do |f| %>
        <div class="field">
          <%= f.label :name %>
          <%= f.text_field :name %>
        </div>

        <div class="field">
          <%= f.label :description %>
          <%= f.text_field :description %>
        </div>

        <%= f.submit t("admin.subject.savechange"), class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>
